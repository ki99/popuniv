version: '3.8'
services:
  vite-dev:
    build:
      context: .
    image: node:20
    working_dir: /app
    ports:
      - '5173:5173'
    environment:
      - VITE_HOST=0.0.0.0
    volumes:
      - .:/app
      - /app/node_modules
    command: ['pnpm', 'dev', '--', '--host']
